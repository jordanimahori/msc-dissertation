# Earth Engine must be configured to use your Earth Engine account before subsequent operations can be performed.
# This only needs to be done once.

import ee

# Trigger the authentication prompt.
ee.Authenticate()

# Initialize
ee.Initialize()
